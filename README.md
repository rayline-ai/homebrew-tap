# Rayline Homebrew Tap

This tap is updated by the Rayline CLI release workflow in
`atlasfutures/memex-desktop`.

Install command after the first promoted release:

```sh
brew install rayline-ai/tap/rayline
```
